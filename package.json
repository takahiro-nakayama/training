{
  "name": "training",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel src/index.html -p 8888 --open -d dist",
    "build": "rm docs/* ;parcel build --no-cache --no-source-maps src/index.html -d docs --public-url ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jquery": "^3.4.1",
    "jquery-ui-dist": "^1.12.1",
    "parcel-bundler": "^1.12.3"
  },
  "devDependencies": {
    "sass": "^1.22.9"
  }
}
